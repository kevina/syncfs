{
  /* required, the folder to use */
  "remote_dir" : "syncfs-storage"

  /* file used to store authorization info */
  //"drive_auth" : ".var/drive.auth",
  
  /* Set these to use your own client credentials rather than the
     builtin ones.  You can create your own for free at
     https://console.developers.google.com */
  //"client_id": "",
  //"client_secret": ""
  //"redirect": "urn:ietf:wg:oauth:2.0:oob"

  /* Use this to set the scope of allowed privileges, useful ones might be:
       https://www.googleapis.com/auth/drive -- Full, permissive scope
       https://www.googleapis.com/auth/drive.readonly -- Readonly
       https://www.googleapis.com/auth/drive.file -- Per-file access to files created or opened by the app
     (see https://developers.google.com/drive/v3/web/about-auth)
     For this option to take effect the driva.auth file should also be
     removed to forse reauthorization.

     "drive.file" can be useful to add an extra layer of security,
     especially when your own client credentials are used.
     Unfortually, as of 2016-02-23 moving files to the trash is
     currently broken so the use_delete option will need to also be
     set. */
  //"scope" : "https://www.googleapis.com/auth/drive.file"

  /* Normally when deleting, files are moved to the trash.  Set this
     to instead permanently delete the files.  If this option is not
     set than you might want to clean out the trash from time to time
     to avoid using up your quota.  This can be done using the
     "--empty-trash" command line option. */
  //"use_delete" : false
}


